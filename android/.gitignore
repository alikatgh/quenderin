# Gradle / Kotlin / Android build output
.gradle/
build/
*.jar
# …but the Gradle wrapper jar must be committed so ./gradlew works on a clean clone.
!gradle/wrapper/gradle-wrapper.jar
*.iml
local.properties
.idea/

# Release signing — NEVER commit the keystore or its passwords (see docs/RELEASE.md).
keystore.properties
*.jks
*.keystore
*.p12
captures/
.cxx/

# External llama.cpp checkout (add as a submodule; not vendored here)
jni/llama.cpp
