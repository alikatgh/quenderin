# --- Quenderin Specific ---
# Ignore the large, downloaded LLM models
# This is the most important line for your use case.
/models/
window_dump.xml
off-grid-mobile/
# Abandoned React Native scaffold — its tracked files were removed upstream (68d8d49); the
# leftover working-tree cruft (~513 MB: node_modules, Pods, vendor, build output) is regenerable,
# not source, and must not be swept into commits/PRs.
/mobile/

# --- Dependencies ---
# Ignore the directory where npm installs packages.
node_modules/

# --- Build output ---
# Ignore compiled TypeScript/JavaScript files if you have a build step.
dist/
build/
out/
release/
# public/ is the built UI served by Express — must be tracked
# Rebuild with: cd ui && npm run build

# --- Logs ---
# Ignore log files.
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Environment variables ---
# Ignore environment-specific files.
# NEVER commit secret keys.
.env
.env.*
!.env.example

# --- OS-specific files ---
# Ignore files generated by macOS, Windows, and Linux systems.
.DS_Store
Thumbs.db
ehthumbs.db
._*

# --- IDE / Editor Directories ---
# Ignore settings for editors like VS Code, JetBrains, etc.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# --- Claude Code ---
# Personal, machine-specific permissions/paths — never share.
.claude/settings.local.json
# Preview/dev-server launch config — holds an absolute, machine-specific path.
.claude/launch.json
# test coverage output
coverage/

# Local config that may hold secrets (never commit)
quenderin.json
